---
name: branch-update
description: 分岐条件表の原文(多くは中国語wiki)を渡して「nn-n を更新して」と依頼されたときの、海域まるごと更新手順。src/core/branch のロジック・public/originalBranchTexts の原文txt・src/data/branch.ts の表示テキストの3点を一括更新する。「62-4を更新して」のようにマップ番号＋分岐表(特に中国語原文)で更新を求められたら、着手前に必ずこのスキルを参照すること。
---

# 分岐 海域まるごと更新 (compass)

ユーザーが分岐条件表(多くは中国語wikiの原文)とともに「`<W>-<M>` を更新して」と依頼したら、**以下の3ファイル群をすべて更新する**。どれか1つでも欠けるとロジックと表示が不整合になる。

## 更新対象(3点セット)

1. **ロジック**: `src/core/branch/world<W>/<W>-<M>.ts` の `calc_<W>_<M>`
2. **原文txt**: `public/originalBranchTexts/world<W>/<W>-<M>.txt`
3. **表示テキスト**: `src/data/branch.ts` の `BRANCH_TEXTS["<W>-<M>"]` と `BRANCH_LAST_UPDATES["<W>-<M>"]`

## 手順

1. 新しい分岐表と現行実装(現行の `.txt` = 直近版)をノード単位で突き合わせ、変更点を洗い出す。
2. `branch-implementation` スキルに従って `calc_<W>_<M>` を更新する。未指定組合せや「索敵値のみ・要求索敵値不明」時の扱いなどのルールもそのスキルに従う。
3. `branch-text-reflection` スキルに従って、`.txt` を新表に書き換え、`branch.ts` に反映する。原文にソース更新日時(例: 「在YYYY-MM-DD HH:MM修改」)があれば `BRANCH_LAST_UPDATES` にその日時を転記する。
4. 型チェックが通ることを確認する。ブラウザでの実画面確認は不要(ユーザーが行う)。

## 注意

- 表に数値が無い等の曖昧点は推測で埋めず確認する(各サブスキルのルールに従う)。
- 常に最小差分。関係ない箇所のリファクタ・整形はしない。
- 個別に「ロジックだけ」「txtだけ反映」と指示された場合は、対応するサブスキル単体でよい。3点セットの一括更新は「原文を渡して海域を更新して」という依頼のときの既定動作。
